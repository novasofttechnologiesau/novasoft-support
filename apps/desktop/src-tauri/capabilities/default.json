{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability granting the main window access to NovaSoft's own diagnostic/action commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
